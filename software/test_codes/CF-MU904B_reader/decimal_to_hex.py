def decimal_to_hex(decimal_number):
    hex_representation = hex(decimal_number)
    return hex_representation

# Example usage:
decimal_input = 4

hex_result = decimal_to_hex(decimal_input)

print(f"Decimal: {decimal_input}\nHexadecimal representation: {hex_result}")
